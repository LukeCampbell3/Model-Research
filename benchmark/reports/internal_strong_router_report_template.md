# PVR-EC-O Internal Strong-Router Control Report Template

The custom fixed MoE belongs only in the internal strong-router control lane.

It is:

- internal champion
- strong-router upper-bound
- teacher diagnostic

It is not:

- generic MoE baseline
- primary generalized baseline
- primary pass/fail baseline

Valid interpretations:

- PVR-EC-O beats generalized baselines but lags internal strong-router control.
- PVR-EC-O matches internal strong-router control.
- PVR-EC-O beats internal strong-router control.
